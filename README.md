نکات مربوط به پروژه :

این پروژه با معماری cqrs و clean architect با رویکرد DDD طراحی شده و تمامی  domain  به صورت rich  پیاده سازی گردیده است 

درپروژه global Exception  استفاده شده که به عنوان handler  به GRPC اضافه شده و اگر رویدادی در سیستم به طور اشتباه exception  اتفاق بیفتد به این handler  انتقال پیدا میکند و نکته دیگر این کهدر ورژن های قدیمی تر میتوانیم در middleware  ها هم exception را تعریف کنیم

نکته بعدی این که در سیستم لاگ را هم به عنوان concern cross cutting  میتوانیم به سیستم اضافه کنیم 

نکته بعدی در مورد validate  کردن پارامترهای ورودی  grpc  هست که به ه دلیایل زیر بهتر است در  command و query انجام پذیرد

کلاس generated است

نگهداری آن سخت می‌شود

نکته بعدی در مورد ارتباط با grpc  هست که به صورت UNARY  طراحی شده و در صورت نیاز میتوان به bidirectional یا client ویا  server stream  تغییر یابد



